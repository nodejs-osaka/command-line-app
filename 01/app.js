/**
 * # 普通のNode.js　スクリプト
 *
 * スタートの前にsay-moreのツールをインストールのが必要です。
 *
 * ```
 *   npm install say-more -g
 * ```
 *
 * Macのターミナルで
 *   
 * ```
 *   node app.js
 * ```
 * 
 * を呼ぶと何かが出っている。（スピーカー忘れないでください）
 */
require("child_process").exec("say-more -f ../a");　